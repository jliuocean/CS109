import itertools
import math
# def main():
#     print(math.factorial(4) / math.factorial(2))
    
#     leters = ['b', 'o', 'b', 'a']
#     perms = set(itertools.permutations(leters))
#     for perm in perms:
#         permStr = "".join(perm)
#         print(permStr)

import math
def main():
    n = math.factorial(4)
    d = math.factorial(2)
    print(n / d)

main()